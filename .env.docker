# WPS - Environment Configuration Example
# Copy this file to appropriate location based on your environment:
# - Local development: .env.docker
# - Staging (CI/CD): .env.staging
# - Production: .env.production

############################
# Environment
############################
APP_ENV=development
APP_DEBUG=true

############################
# Application
############################
APP_NAME=WPS
APP_KEY=base64:2FPjQajGEZfwEOPNm1Kb0xUA9ZeMh7WdpG8/4hJio2k=

############################
# Domain & URLs
# Change DOMAIN_NAME for different environments
############################
DOMAIN_NAME=wps.test
APP_URL=https://wps.test

# For staging/demo environment
# DOMAIN_NAME=staging.wps.test
# APP_URL=https://staging.wps.test

# For production environment
# DOMAIN_NAME=wps.example.com
# APP_URL=https://wps.example.com

############################
# Locale & Timezone
############################
APP_LOCALE=ru
APP_FALLBACK_LOCALE=ru
APP_FAKER_LOCALE=ru_RU

############################
# Database
############################
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=wps
DB_USERNAME=postgres
DB_PASSWORD=postgres

############################
# Redis Cache
############################
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null
CACHE_STORE=redis
CACHE_DRIVER=redis

############################
# Session & Cookies
############################
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/

############################
# Queue & Broadcasting
############################
QUEUE_CONNECTION=redis
BROADCAST_CONNECTION=log

############################
# File Storage
############################
FILESYSTEM_DISK=local

############################
# CORS & External Access
############################
# Update for your domain
CORS_ALLOWED_ORIGINS=https://wps.test,http://localhost,http://localhost:3000,http://127.0.0.1

# For staging
# CORS_ALLOWED_ORIGINS=https://staging.wps.test,https://wps.test

# For production
# CORS_ALLOWED_ORIGINS=https://wps.example.com

############################
# Frontend API URL
############################
VITE_APP_NAME=WPS
VITE_API_BASE_URL=/api

# For staging (if using full URL, be careful with CORS and proxies)
# VITE_API_BASE_URL=https://staging.wps.test/api

# For production (if using full URL, be careful with CORS and proxies)
# VITE_API_BASE_URL=https://wps.example.com/api

############################
# Mail
############################
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

############################
# Admin Panel
############################
ADMIN_EMAIL=admin
ADMIN_PASSWORD=secret

############################
# Security & Encryption
############################
BCRYPT_ROUNDS=12

############################
# Logging
############################
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=info

# For development
LOG_LEVEL=debug

############################
# AWS S3 (if using)
############################
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

############################
# External Services
############################
# Add any third-party API keys here
# STRIPE_KEY=
# TWILIO_ACCOUNT_SID=
# etc.
