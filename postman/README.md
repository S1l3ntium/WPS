# WPS API - Postman Collection

Полная коллекция запросов для тестирования WPS API в Postman.

## 📋 Содержание

```
postman/
├── collections/
│   └── WPS_API.postman_collection.json    # Коллекция со всеми API endpoints
├── environments/
│   ├── Development.postman_environment.json # Окружение для локальной разработки
│   ├── Staging.postman_environment.json     # Окружение для staging сервера
│   └── Production.postman_environment.json  # Окружение для production
└── README.md                                # Этот файл
```

## 🚀 Быстрый старт

### 1. Импортируем коллекцию в Postman

1. Откройте Postman
2. Нажмите "Import" в левом меню
3. Выберите файл `collections/WPS_API.postman_collection.json`
4. Нажмите "Import"

### 2. Импортируем окружение

1. Нажмите иконку "Environments" в левом меню
2. Нажмите "Import"
3. Выберите файл из `environments/` папки (например, Development)
4. Нажмите "Import"

### 3. Выбираем окружение

1. В верхнем правом углу найдите dropdown с названием окружения
2. Выберите нужное окружение (Development, Staging, Production)

## 📊 Структура коллекции

### Events
- **GET /api/events** - Получить все события
- **GET /api/events?date=2025-09-20** - Фильтр по дате
- **GET /api/events?tags=culture,education** - Фильтр по тегам
- **GET /api/events/{id}** - Деталь события
- **POST /api/events** - Создать событие (admin)
- **PUT /api/events/{id}** - Обновить событие (admin)
- **DELETE /api/events/{id}** - Удалить событие (admin)

### News
- **GET /api/news** - Получить все новости
- **GET /api/news?type=article** - Фильтр по типу
- **GET /api/news/{id}** - Деталь новости
- **POST /api/news** - Создать новость (admin)

### Partners
- **GET /api/partners** - Получить всех партнеров
- **GET /api/partners/{id}** - Деталь партнера

### Partner Packages
- **GET /api/partner-packages** - Получить пакеты
- **GET /api/partner-packages?category=strategic** - Фильтр по категории

### Hotels
- **GET /api/hotels** - Получить отели
- **GET /api/hotels?category=recommended** - Фильтр по категории

### Committee Members
- **GET /api/committee-members** - Получить членов комитета
- **GET /api/committee-members/{id}** - Деталь члена комитета

### Competitions
- **GET /api/competitions** - Получить конкурсы
- **GET /api/competitions/{id}** - Деталь конкурса с FAQ

### Awards
- **GET /api/awards** - Получить награды
- **GET /api/awards?year=2025** - Фильтр по году
- **GET /api/awards/{id}** - Деталь награды

## 🔧 Настройка переменных

### Development окружение

Переменные уже настроены для локального сервера:

```json
{
  "base_url": "http://localhost:8000",
  "api_url": "http://localhost:8000/api",
  "token": "",
  "admin_email": "admin@example.com",
  "admin_password": ""
}
```

**Действия:**
1. Убедитесь, что Laravel сервер запущен: `php artisan serve`
2. Сервер должен быть доступен на `http://localhost:8000`
3. Все GET запросы должны работать сразу

### Production окружение

```json
{
  "base_url": "https://api.worldpublicsummit.com",
  "api_url": "https://api.worldpublicsummit.com/api",
  "token": "",
  "admin_email": "admin@worldpublicsummit.com",
  "admin_password": ""
}
```

**Действия:**
1. Обновите `admin_email` и `admin_password` с реальными credentials
2. Установите `token` после успешной аутентификации (если требуется)

### Staging окружение

```json
{
  "base_url": "https://staging-api.worldpublicsummit.com",
  "api_url": "https://staging-api.worldpublicsummit.com/api",
  "token": "",
  "admin_email": "admin@staging.worldpublicsummit.com",
  "admin_password": ""
}
```

## ✅ Тестирование запросов

### Публичные endpoints (без аутентификации)

Все GET запросы работают без токена:

1. Выберите окружение (Development)
2. Нажмите на любой GET запрос
3. Нажмите "Send"
4. Проверьте ответ в разделе "Response"

**Пример:**
```
GET http://localhost:8000/api/events
Response: 200 OK
{
  "data": [
    {
      "id": 1,
      "title": {"ru": "...", "en": "..."},
      ...
    }
  ]
}
```

### Admin endpoints (с аутентификацией)

Для POST, PUT, DELETE запросов требуется токен:

1. Добавьте `Authorization: Bearer {{token}}` в headers
2. Получите токен от сервера аутентификации
3. Установите значение `{{token}}` в окружении
4. Отправляйте запросы

**Пример POST запроса:**
```json
POST http://localhost:8000/api/events
Header: Authorization: Bearer your_token_here
Body: {
  "title": {"ru": "Новое событие", "en": "New Event"},
  "start_date": "2025-09-22 09:00:00",
  ...
}
Response: 201 Created
```

## 🔐 Аутентификация

### Для Development

Сейчас аутентификация не обязательна для тестирования. Все endpoints доступны.

Если добавите аутентификацию:
1. Реализуйте endpoint для получения токена
2. Добавьте pre-request script в Postman для автоматического получения токена
3. Используйте {{token}} в запросах

### Для Production

1. Используйте реальные credentials
2. Получите токен от API
3. Обновите {{token}} в окружении
4. Используйте в заголовке Authorization

## 📝 Примеры использования

### Получить все события

```bash
curl -X GET http://localhost:8000/api/events
```

Response:
```json
{
  "data": [
    {
      "id": 1,
      "title": {
        "ru": "Стратегическая сессия 1",
        "en": "Strategic Session 1"
      },
      "description": {
        "ru": "Описание события на русском",
        "en": "Event description in English"
      },
      "type": "СТРАТЕГИЧЕСКАЯ СЕССИЯ",
      "date": "19.09.2025",
      "time": "09:00 - 10:30",
      "location": {
        "ru": "Конгресс-центр CMT",
        "en": "CMT Congress Center"
      },
      "tags": ["culture", "education"],
      "downloadLink": null
    }
  ]
}
```

### Получить событие по дате

```bash
curl -X GET "http://localhost:8000/api/events?date=2025-09-20"
```

### Получить события по тегам

```bash
curl -X GET "http://localhost:8000/api/events?tags=culture,education"
```

### Создать новое событие

```bash
curl -X POST http://localhost:8000/api/events \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer your_token" \
  -d '{
    "title": {"ru": "Новое событие", "en": "New Event"},
    "description": {"ru": "Описание", "en": "Description"},
    "type": "ПАНЕЛЬНАЯ ДИСКУССИЯ",
    "start_date": "2025-09-22 09:00:00",
    "end_date": "2025-09-22 10:30:00",
    "location": {"ru": "Конгресс-центр", "en": "Congress Center"},
    "venue": {"ru": "Зал А", "en": "Hall A"},
    "tags": ["culture"],
    "goals": ["Goal 1"],
    "format": {"ru": "Формат", "en": "Format"},
    "discussion_questions": ["Question 1"]
  }'
```

## 🐛 Troubleshooting

### Ошибка: "Cannot GET /api/events"

**Причина:** Laravel сервер не запущен или неправильный URL

**Решение:**
1. Убедитесь, что Laravel запущен: `php artisan serve`
2. Проверьте URL в окружении (должен быть `http://localhost:8000`)
3. Проверьте, что routes/api.php загружена в bootstrap/app.php

### Ошибка: "401 Unauthorized"

**Причина:** Токен отсутствует или истек

**Решение:**
1. Проверьте, что {{token}} установлен в окружении
2. Получите новый токен от сервера
3. Обновите {{token}} в окружении

### Ошибка: "422 Unprocessable Entity"

**Причина:** Ошибка валидации данных

**Решение:**
1. Проверьте структуру JSON в теле запроса
2. Убедитесь, что все требуемые поля присутствуют
3. Проверьте форматы данных (даты, числа и т.д.)

## 📚 Дополнительные ресурсы

- [API_DOCUMENTATION.md](../../wps-laravel/API_DOCUMENTATION.md) - Полная документация API
- [MIGRATION_NOTES.md](../../wps-laravel/MIGRATION_NOTES.md) - Заметки о миграции
- [Postman Documentation](https://learning.postman.com/) - Документация Postman

## 💡 Советы

1. **Используйте переменные окружения** - Это позволяет легко переключаться между dev/staging/prod
2. **Сохраняйте responses** - Postman может сохранить примеры ответов для документации
3. **Создавайте тесты** - Добавьте tests в каждый запрос для автоматической валидации
4. **Документируйте endpoints** - Добавьте описания для каждого запроса
5. **Экспортируйте для других** - Поделитесь коллекцией с командой

## 📧 Контакты

При проблемах с API обратитесь к документации или командде разработки.

---

**Последнее обновление:** 2025-12-20
**Версия:** 1.0.0
