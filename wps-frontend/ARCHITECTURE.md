# Архитектура приложения

## 🏗 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      BROWSER                                │
│              https://worldpublicsummit.test                 │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTPS
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  NGINX (443)                                │
│          /opt/homebrew/etc/nginx/...                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────┐    ┌──────────────────────────┐    │
│  │   React Frontend    │    │   API Proxy (/api/)      │    │
│  │   dist/index.html   │    │ → http://localhost:8000  │    │
│  │   dist/assets/*     │    └──────────────────────────┘    │
│  │   Cache: 7 days     │              │                     │
│  └─────────────────────┘              │ HTTP                │
│                                       ▼                     │
│                          ┌──────────────────────────┐        │
│                          │  Laravel API             │        │
│                          │  Port: 8000              │        │
│                          └──────────────────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 📱 Frontend Stack

```
React 18
├── React Router 7 (маршрутизация)
├── Vite 6 (сборка)
├── TypeScript (типизация)
├── Tailwind CSS (стили)
├── Radix UI (компоненты)
└── useApi Hook (работа с API)
```

## 🛣 Маршруты приложения

```
/                    → HomePage
├── /partners         → PartnersPage
├── /program          → ProgramPage
│   ├── /event/:id    → EventPage (детали события)
├── /participants     → ParticipantsPage
├── /award            → AwardPage
├── /grants-comp.     → GrantsCompetitionPage
├── /leadership-comp. → LeadershipCompetitionPage
├── /press-center     → PressCenterPage
│   ├── /news/:id     → NewsPage (детали новости)
├── /photo-gallery    → PhotoGalleryPage
├── /venue            → VenuePage
├── /contacts         → ContactsPage
├── /about            → AboutPage
│   ├── /mission      → MissionPage
│   ├── /org-comm.    → OrgCommitteePage
│   └── /organizers   → OrganizersPage
└── /mobile-app       → MobileAppPage
```

## 🔄 Data Flow

```
┌──────────────────┐
│   React Router   │
│  (маршрутизация) │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│   Компонент      │
│   (страница)     │
└────────┬─────────┘
         │ используется
         ▼
┌──────────────────────────┐
│   useApi Hook            │
│ - get('/api/...')        │
│ - post('/api/...', data) │
│ - put, delete            │
└────────┬─────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   Nginx Proxy                    │
│   /api/* → http://localhost:8000 │
└────────┬────────────────────────┘
         │
         ▼
┌──────────────────────┐
│   Laravel API        │
│   - GET /api/events  │
│   - POST /api/events │
│   - PUT /api/events  │
│   - DELETE /api/...  │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│   Database           │
│   (MySQL/PostgreSQL) │
└──────────────────────┘
```

## 📁 Структура проекта

```
wps-frontend/
│
├── src/
│   ├── app/
│   │   ├── routes.tsx              ← Определение маршрутов
│   │   ├── App.tsx                 ← Главный компонент
│   │   ├── components/
│   │   │   ├── HomePage.tsx
│   │   │   ├── PartnersPage.tsx
│   │   │   ├── EventPage.tsx
│   │   │   ├── NewsPage.tsx
│   │   │   ├── Header.tsx          ← Общий хедер
│   │   │   ├── Footer.tsx          ← Общий футер
│   │   │   └── ... (16 других компонентов)
│   │   ├── ErrorBoundary.tsx
│   │   └── components/ui/          ← Radix UI компоненты
│   │
│   ├── hooks/
│   │   └── useApi.ts               ← Hook для API запросов
│   │
│   ├── styles/
│   │   └── index.css
│   │
│   └── main.tsx                    ← Entry point
│
├── dist/                            ← Production build
│   ├── index.html
│   └── assets/
│       ├── *.js
│       └── *.css
│
├── vite.config.ts                   ← Vite конфиг (proxy, SSL)
├── .env.development                 ← Dev переменные
├── .env.production                  ← Prod переменные
├── package.json                     ← Dependencies
└── tailwind.config.ts               ← Tailwind конфиг
```

## 🔗 API Endpoints

```
GET    /api/events           ← Список событий
GET    /api/events/:id       ← Деталь события
POST   /api/events           ← Создать событие
PUT    /api/events/:id       ← Обновить событие
DELETE /api/events/:id       ← Удалить событие

GET    /api/news             ← Список новостей
GET    /api/news/:id         ← Деталь новости
POST   /api/news             ← Создать новость
PUT    /api/news/:id         ← Обновить новость
DELETE /api/news/:id         ← Удалить новость

GET    /api/partners         ← Список партнеров
GET    /api/participants     ← Список участников
... (и другие endpoint'ы)
```

## 🔄 Жизненный цикл запроса

```
1. Пользователь открывает https://worldpublicsummit.test/events/123
   │
   ▼
2. Nginx:
   - Проверяет /events/123 в dist/ ❌
   - try_files $uri $uri/ /index.html
   - Возвращает index.html ✅
   │
   ▼
3. React загружается в браузере
   - BrowserRouter инициализируется
   - useRoutes читает текущий URL: /events/123
   - Совпадает с маршрутом /event/:eventId
   │
   ▼
4. Загружается EventPage компонент
   - useParams() получает eventId = 123
   - useApi().get('/api/events/123')
   │
   ▼
5. API запрос:
   - Фронтенд: GET https://worldpublicsummit.test/api/events/123
   - Nginx proxy: → http://localhost:8000/events/123
   - Laravel API обрабатывает запрос
   - Возвращает JSON
   │
   ▼
6. Данные отображаются на странице
   - useState обновляет состояние
   - Компонент переренд... ренджится
```

## 🚀 Deployment Flow

```
1. npm run build
   ↓
2. Файлы собираются в dist/
   ↓
3. Nginx перезагружается
   sudo nginx -s reload
   ↓
4. Nginx обслуживает dist/index.html и assets/
   ↓
5. Пользователи обращаются к https://worldpublicsummit.test
   ↓
6. Все работает! ✅
```

## 🔒 Безопасность

```
Browser
  │ HTTPS (зашифровано)
  ▼
Nginx
  │ CORS headers (проверка origin)
  │ X-Frame-Options: DENY
  │ X-Content-Type-Options: nosniff
  │ HSTS (принудительно HTTPS)
  ▼
Laravel API
  │ Валидация входных данных
  │ Authentication (если нужно)
  │ Authorization (roles/permissions)
  ▼
Database
```

## 📊 Performance

```
Frontend:
- Gzip compression ✅
- Static assets cache: 7 дней ✅
- CSS/JS минификация ✅
- Lazy loading components (опционально) ⚠️

Backend:
- Database indexing ⚠️
- Query optimization ⚠️
- Response caching ⚠️
```

## 🛠 Development vs Production

```
Development:
- npm run dev
- http://worldpublicsummit.test:5173
- Hot Module Reload
- Source maps
- Dev tools включены

Production:
- npm run build
- https://worldpublicsummit.test
- Минифицированный код
- No source maps
- Optimized assets
```

## 📈 Масштабирование

```
Текущая архитектура:
Один Nginx + Один Laravel сервер

Если нужно масштабировать:

Option 1: Кэширование
├── Redis для session'ов
├── Cache для часто используемых данных
└── CDN для статики

Option 2: Load Balancing
├── Несколько Laravel серверов
├── Nginx upstream с балансировкой
└── Shared database

Option 3: Микросервисы
├── API Gateway (Nginx)
├── Несколько сервисов (Events, News, Users, etc)
└── Message queue (RabbitMQ/Redis)
```

## 🎯 Компоненты системы

| Компонент | Роль | Статус |
|-----------|------|--------|
| React | Frontend UI | ✅ |
| React Router | Маршрутизация | ✅ |
| Vite | Build tool | ✅ |
| Nginx | Reverse proxy, Static serving | ✅ |
| Laravel | REST API | ✅ |
| Database | Data storage | ⚠️ (в бэкенде) |
| SSL/TLS | Encryption | ✅ |
| CORS | Security | ✅ |

## 📝 Docs в проекте

- `NGINX_SETUP.md` - Как работает Nginx конфиг
- `ROUTER_SETUP.md` - React Router конфигурация
- `API_CONFIG.md` - API конфигурация
- `DEPLOYMENT_SUMMARY.md` - Весь процесс deployment'а
- `PRODUCTION_CHECKLIST.md` - Быстрый чек-лист перед production
