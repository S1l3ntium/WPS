# Архитектура приложения

## 🏗 Общая архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                      BROWSER                                │
│              https://worldpublicsummit.test                 │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTPS
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  NGINX (443)                                │
│          /opt/homebrew/etc/nginx/...                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────┐    ┌──────────────────────────┐    │
│  │   React Frontend    │    │   API Proxy (/api/)      │    │
│  │   dist/index.html   │    │ → http://localhost:8000  │    │
│  │   dist/assets/*     │    └──────────────────────────┘    │
│  │   Cache: 7 days     │              │                     │
│  └─────────────────────┘              │ HTTP                │
│                                       ▼                     │
│                          ┌──────────────────────────┐        │
│                          │  Laravel API             │        │
│                          │  Port: 8000              │        │
│                          └──────────────────────────┘        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 📱 Frontend Stack

```
React 19
├── React Router 7 (маршрутизация с locale параметром)
├── Vite 6 (сборка)
├── TypeScript 5 (типизация)
├── Tailwind CSS (утилитарный CSS)
├── lucide-react (иконки)
├── React Context API (глобальное состояние)
│   └── LocaleContext (управление языком и URL)
├── Custom Hooks
│   ├── useTranslation() (доступ к переводам)
│   ├── useApi() (API запросы)
│   └── useLocaleNavigate() (навигация с locale)
└── i18n система (translations.ts - 150+ ключей RU/EN)
```

## 🛣 Маршруты приложения

```
/                    → HomePage
├── /partners         → PartnersPage
├── /program          → ProgramPage
│   ├── /event/:id    → EventPage (детали события)
├── /participants     → ParticipantsPage
├── /award            → AwardPage
├── /grants-comp.     → GrantsCompetitionPage
├── /leadership-comp. → LeadershipCompetitionPage
├── /press-center     → PressCenterPage
│   ├── /news/:id     → NewsPage (детали новости)
├── /photo-gallery    → PhotoGalleryPage
├── /venue            → VenuePage
├── /contacts         → ContactsPage
├── /about            → AboutPage
│   ├── /mission      → MissionPage
│   ├── /org-comm.    → OrgCommitteePage
│   └── /organizers   → OrganizersPage
└── /mobile-app       → MobileAppPage
```

## 🔄 Data Flow

```
┌──────────────────┐
│   React Router   │
│  (маршрутизация) │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│   Компонент      │
│   (страница)     │
└────────┬─────────┘
         │ используется
         ▼
┌──────────────────────────┐
│   useApi Hook            │
│ - get('/api/...')        │
│ - post('/api/...', data) │
│ - put, delete            │
└────────┬─────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│   Nginx Proxy                    │
│   /api/* → http://localhost:8000 │
└────────┬────────────────────────┘
         │
         ▼
┌──────────────────────┐
│   Laravel API        │
│   - GET /api/events  │
│   - POST /api/events │
│   - PUT /api/events  │
│   - DELETE /api/...  │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│   Database           │
│   (MySQL/PostgreSQL) │
└──────────────────────┘
```

## 📁 Структура проекта

```
wps-frontend/
│
├── src/
│   ├── app/
│   │   ├── routes.tsx              ← Маршруты с :locale параметром
│   │   ├── App.tsx                 ← Главный компонент
│   │   ├── ErrorBoundary.tsx       ← Error handling
│   │   └── components/
│   │       ├── Header.tsx          ← Навигация (локализирована)
│   │       ├── Footer.tsx          ← Подвал (локализирован)
│   │       ├── HomePage.tsx        ← 1/19 Главная
│   │       ├── AboutPage.tsx       ← 2/19 О проекте
│   │       ├── MissionPage.tsx     ← 3/19 Миссия
│   │       ├── ContactsPage.tsx    ← 4/19 Контакты (локализирована)
│   │       ├── ProgramPage.tsx     ← 5/19 Программа
│   │       ├── EventPage.tsx       ← 6/19 Детали события
│   │       ├── NewsPage.tsx        ← 7/19 Новости
│   │       ├── PressCenterPage.tsx ← 8/19 Пресс-центр
│   │       ├── PartnersPage.tsx    ← 9/19 Партнеры
│   │       ├── ParticipantsPage.tsx← 10/19 Для участников
│   │       ├── AwardPage.tsx       ← 11/19 Награды
│   │       ├── VenuePage.tsx       ← 12/19 Место проведения
│   │       ├── GrantsCompetition.. ← 13/19 Конкурс грантов
│   │       ├── Leadership Compet.. ← 14/19 Конкурс лидерства
│   │       ├── OrganizersPage.tsx  ← 15/19 Организаторы
│   │       ├── OrgCommitteePage.tsx← 16/19 Комитет
│   │       ├── MobileAppPage.tsx   ← 17/19 Мобильное приложение
│   │       ├── PhotoGalleryPage.tsx← 18/19 Фотогалерея
│   │       └── PhotoGallery.tsx    ← 19/19 Фото (компонент)
│   │
│   ├── context/
│   │   └── LocaleContext.tsx       ← Глобальное состояние языка
│   │
│   ├── hooks/
│   │   ├── useApi.ts               ← API запросы (GET/POST/PUT/DELETE)
│   │   ├── useLocaleNavigate.ts    ← Навигация с locale
│   │   └── (другие)
│   │
│   ├── i18n/
│   │   ├── translations.ts         ← 150+ ключей (RU + EN)
│   │   └── useTranslation.ts       ← Hook для переводов
│   │
│   ├── services/
│   │   └── api.ts                  ← Конфигурация API
│   │
│   ├── styles/
│   │   ├── index.css
│   │   ├── tailwind.css
│   │   └── theme.css               ← Цветовая схема
│   │
│   └── main.tsx                    ← Entry point
│
├── dist/                           ← Production build
│   ├── index.html
│   └── assets/
│       ├── *.js
│       └── *.css
│
├── vite.config.ts                  ← Vite (proxy на :8000, SSL, HTTPS)
├── .env                            ← Dev окружение (не в git)
├── .env.example                    ← Шаблон
├── package.json                    ← Dependencies
└── tailwind.config.ts              ← Tailwind конфиг
```

## 🔗 API Endpoints

```
GET    /api/events           ← Список событий
GET    /api/events/:id       ← Деталь события
POST   /api/events           ← Создать событие
PUT    /api/events/:id       ← Обновить событие
DELETE /api/events/:id       ← Удалить событие

GET    /api/news             ← Список новостей
GET    /api/news/:id         ← Деталь новости
POST   /api/news             ← Создать новость
PUT    /api/news/:id         ← Обновить новость
DELETE /api/news/:id         ← Удалить новость

GET    /api/partners         ← Список партнеров
GET    /api/participants     ← Список участников
... (и другие endpoint'ы)
```

## 🔄 Жизненный цикл запроса

```
1. Пользователь открывает https://worldpublicsummit.test/events/123
   │
   ▼
2. Nginx:
   - Проверяет /events/123 в dist/ ❌
   - try_files $uri $uri/ /index.html
   - Возвращает index.html ✅
   │
   ▼
3. React загружается в браузере
   - BrowserRouter инициализируется
   - useRoutes читает текущий URL: /events/123
   - Совпадает с маршрутом /event/:eventId
   │
   ▼
4. Загружается EventPage компонент
   - useParams() получает eventId = 123
   - useApi().get('/api/events/123')
   │
   ▼
5. API запрос:
   - Фронтенд: GET https://worldpublicsummit.test/api/events/123
   - Nginx proxy: → http://localhost:8000/events/123
   - Laravel API обрабатывает запрос
   - Возвращает JSON
   │
   ▼
6. Данные отображаются на странице
   - useState обновляет состояние
   - Компонент переренд... ренджится
```

## 🚀 Deployment Flow

```
1. npm run build
   ↓
2. Файлы собираются в dist/
   ↓
3. Nginx перезагружается
   sudo nginx -s reload
   ↓
4. Nginx обслуживает dist/index.html и assets/
   ↓
5. Пользователи обращаются к https://worldpublicsummit.test
   ↓
6. Все работает! ✅
```

## 🔒 Безопасность

```
Browser
  │ HTTPS (зашифровано)
  ▼
Nginx
  │ CORS headers (проверка origin)
  │ X-Frame-Options: DENY
  │ X-Content-Type-Options: nosniff
  │ HSTS (принудительно HTTPS)
  ▼
Laravel API
  │ Валидация входных данных
  │ Authentication (если нужно)
  │ Authorization (roles/permissions)
  ▼
Database
```

## 📊 Performance

```
Frontend:
- Gzip compression ✅
- Static assets cache: 7 дней ✅
- CSS/JS минификация ✅
- Lazy loading components (опционально) ⚠️

Backend:
- Database indexing ⚠️
- Query optimization ⚠️
- Response caching ⚠️
```

## 🛠 Development vs Production

```
Development:
- npm run dev
- http://worldpublicsummit.test:5173
- Hot Module Reload
- Source maps
- Dev tools включены

Production:
- npm run build
- https://worldpublicsummit.test
- Минифицированный код
- No source maps
- Optimized assets
```

## 🔄 Локализация (i18n система)

```
Структура локализации:
┌─────────────────────────────────────┐
│   URL с locale параметром           │
│   /ru/program    /en/news/{id}      │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   LocaleContext (React Context)     │
│   - Читает locale из URL             │
│   - Сохраняет в localStorage         │
│   - Предоставляет глобально         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   useTranslation() hook             │
│   const { t } = useTranslation()    │
│   t('nav.about') → переведённый текст
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│   translations.ts                   │
│   { ru: {...}, en: {...} }          │
│   150+ ключей для всех страниц      │
└─────────────────────────────────────┘
```

**Ключевые компоненты i18n:**

- `LocaleContext.tsx` - Глобальное состояние (читает URL, сохраняет в localStorage)
- `useTranslation()` - Hook для доступа к переводам в компонентах
- `useLocaleNavigate()` - Hook для навигации с автоматическим добавлением locale
- `translations.ts` - Центральное хранилище всех переводов

**Локализированные компоненты (4/19 страниц):**

- ✅ Header.tsx (полностью)
- ✅ Footer.tsx (полностью)
- ✅ MissionPage.tsx (полностью)
- ✅ ContactsPage.tsx (полностью)

**В процессе локализации:**

- 🟡 NewsPage, ProgramPage, EventPage, PressCenterPage, ParticipantsPage

**Требуют локализации:**

- ❌ 10 остальных страниц (частично или полностью)

## 📈 Масштабирование

```
Текущая архитектура:
Nginx (reverse proxy) + React SPA → Laravel API + PostgreSQL

Если нужно масштабировать:

Option 1: Кэширование
├── Redis для session'ов и API cache
├── Cache для часто используемых данных
└── CDN для статических assets (из dist/)

Option 2: Load Balancing
├── Несколько Laravel серверов
├── Nginx upstream с балансировкой
└── Shared database (PostgreSQL replicated)

Option 3: Microservices
├── API Gateway (Nginx)
├── Несколько сервисов (Events, News, Users, etc)
└── Message queue (Redis/RabbitMQ для уведомлений)
```

## 🎯 Компоненты системы

| Компонент | Роль | Версия | Статус |
|-----------|------|--------|--------|
| React | Frontend SPA | 19 | ✅ |
| React Router | Маршрутизация с locale | 7 | ✅ |
| React Context | Global state (locale) | 19 | ✅ |
| Vite | Build tool & dev server | 6 | ✅ |
| Tailwind CSS | Styling | 3+ | ✅ |
| lucide-react | Icons | latest | ✅ |
| Nginx | Reverse proxy, Static serving | latest | ✅ |
| Laravel | REST API Backend | 11 | ✅ |
| PostgreSQL | Database | 14+ | ✅ |
| MoonShine | Admin Panel | latest | ✅ |
| SSL/TLS | HTTPS Encryption | Let's Encrypt | ✅ |
| CORS | Cross-origin security | Configured | ✅ |

## 📚 Документация в проекте

**Frontend:**

- `README.md` - Development guide
- `ARCHITECTURE.md` - Архитектурный обзор (этот файл)
- `LOCALIZATION_COMPLETE.md` - i18n система в деталях

**Backend:**

- `wps-laravel/README.md` - Development guide
- `wps-laravel/API_DOCUMENTATION.md` - Полная API документация
- `wps-laravel/MOONSHINE.md` - Admin panel guide

**Корень проекта:**

- `PROJECT_README.md` - Full-stack overview
