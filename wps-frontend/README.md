# WPS Frontend - World Public Summit

React + TypeScript + Vite приложение для Всемирной Общественной Ассамблеи.

## 🎯 Технологии

- **React 19** - UI фреймворк
- **TypeScript 5** - Type-safe разработка
- **Vite 6** - Build tool и dev server
- **React Router 7** - SPA routing с поддержкой locale
- **Tailwind CSS** - Утилитарный CSS фреймворк
- **i18n система** - Полная локализация RU/EN

## 📦 Установка

```bash
npm install
```

## 🚀 Запуск

### Development mode
```bash
npm run dev
```

Dev server доступен на `https://worldpublicsummit.test` (требуется Nginx proxy с SSL)

### Production build
```bash
npm run build
```

Собранные файлы находятся в `dist/` директории.

## 🌐 Особенности

### Полная локализация (RU/EN)
- URL префиксы `/ru/` и `/en/` для каждого маршрута
- Все UI текст переводится через i18n систему
- localStorage сохраняет выбор пользователя

### Примеры URL:
```
/ru                     → Главная на русском
/en                     → Главная на английском
/ru/program            → Программа на русском
/en/program            → Программа на английском
/ru/news/1             → Новость 1 на русском
/en/news/2             → Новость 2 на английском
```

### API интеграция
- Custom `useApi` hook для всех API запросов
- Настраивается через `VITE_API_URL` environment variable
- Автоматическая обработка ошибок и loading состояний

### Responsive дизайн
- Mobile-first подход
- Tailwind CSS для адаптивности
- Tested на всех основных браузерах

## 📁 Структура проекта

```
src/
├── app/
│   ├── components/          # React компоненты для каждой страницы
│   │   ├── HomePage.tsx
│   │   ├── AboutPage.tsx
│   │   ├── ProgramPage.tsx
│   │   └── ... (всего 19 страниц)
│   ├── routes.tsx           # React Router конфигурация
│   ├── App.tsx              # Root компонент
│   └── ErrorBoundary.tsx    # Error handling
├── context/
│   └── LocaleContext.tsx    # Global locale состояние и логика
├── hooks/
│   ├── useApi.ts            # API запросы
│   └── useLocaleNavigate.ts # Навигация с locale
├── i18n/
│   ├── translations.ts      # Все переводы (RU + EN)
│   └── useTranslation.ts    # Hook для доступа к переводам
├── services/
│   └── api.ts               # API конфигурация
├── styles/
│   ├── index.css
│   ├── tailwind.css
│   └── theme.css            # Цветовая схема
└── main.tsx                 # Точка входа
```

## 🔄 Локализация - как это работает

### Использование переводов в компонентах

```typescript
import { useTranslation } from '../../i18n/useTranslation';

export function MyComponent() {
  const { t, locale } = useTranslation();

  return (
    <h1>{t('homePage.mainTitle')}</h1>  // "Всемирная Общественная Ассамблея"
  );
}
```

### Добавление новых переводов

1. Отредактируй `src/i18n/translations.ts`
2. Добавь ключ для русского и английского текста
3. Используй в компонентах через `t('key')`

### Структура переводов

```typescript
translations = {
  ru: {
    nav: {
      about: "О Всемирной Ассамблее",
      program: "Программа и документы",
    },
    buttons: {
      participate: "Принять участие",
    }
  },
  en: {
    nav: {
      about: "About the World Assembly",
      program: "Program and Documents",
    },
    buttons: {
      participate: "Participate",
    }
  }
}
```

## 🔌 API интеграция

### Использование useApi hook

```typescript
import { useApi } from '../../hooks/useApi';

export function EventsPage() {
  const { eventsAPI } = useApi();
  const [events, setEvents] = useState([]);

  useEffect(() => {
    eventsAPI.getAll().then(setEvents);
  }, []);

  return <div>{events.map(e => <div key={e.id}>{e.title}</div>)}</div>;
}
```

### Доступные API методы

```typescript
eventsAPI.getAll()           // GET /api/events
eventsAPI.getById(id)        // GET /api/events/{id}
newsAPI.getAll()             // GET /api/news
newsAPI.getById(id)          // GET /api/news/{id}
partnersAPI.getAll()         // GET /api/partners
awardsAPI.getAll()           // GET /api/awards
// ... и другие
```

## 🎨 Стилизация

Проект использует **Tailwind CSS** для стилизации.

### Основные цвета (из theme.css):
- Основной: `#1a1f4d` (тёмный синий)
- Акцент: `#4db8b8` (голубой)
- Зелень: `#7dd957` (лайм)

### Пример:
```jsx
<button className="bg-[#4db8b8] hover:bg-[#3da8a8] text-white px-8 py-4 rounded">
  Кнопка
</button>
```

## 🚢 Развёртывание

### Development на локальной машине

1. Убедись, что Nginx настроена с SSL и proxy на `localhost:5173`
2. Запусти `npm run dev`
3. Открой `https://worldpublicsummit.test`

### Production

1. Запусти `npm run build`
2. Скопируй содержимое `dist/` на Nginx сервер
3. Настрой Nginx для SPA (fallback на index.html)

## 📝 Environment variables

### .env файл (создай локально):
```env
VITE_API_URL=http://localhost:8000
VITE_APP_ENV=development
```

### .env.production (для production build):
```env
VITE_API_URL=https://api.worldpublicsummit.org
VITE_APP_ENV=production
```

## 🧪 Тестирование

Проверь локализацию на всех страницах:
- Переключение между RU/EN в header
- URL меняется на `/ru/` или `/en/`
- При обновлении страницы язык сохраняется (localStorage)
- Все тексты переводятся корректно

## 🐛 Проблемы и поддержка

Если возникли проблемы:
1. Проверь, что API доступна (`VITE_API_URL`)
2. Откройте console (F12) и посмотри ошибки
3. Убедись, что Node версия >= 16

## 📄 Дополнительно

- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура приложения
- [API_CONFIG.md](./API_CONFIG.md) - Настройка API
- [LOCALIZATION_COMPLETE.md](./LOCALIZATION_COMPLETE.md) - Подробно о локализации
- [NEXT_STEPS.md](./NEXT_STEPS.md) - Дальнейшие задачи

## 📊 Статус локализации

- ✅ 4 страницы полностью локализированы
- 🟡 6 страниц частично (нужно доделать)
- ❌ 9 страниц нужно локализировать

**Цель**: Все 19 страниц локализированы к моменту production
