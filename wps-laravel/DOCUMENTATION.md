# World Public Summit (WPS) - Всемирная Общественная Ассамблея

Стартовый Laravel проект для переноса сайта https://worldpublicsummit.org

## 📋 О проекте

Это базовый Laravel проект, подготовленный для разработки и расширения функциональности портала Всемирной Общественной Ассамблеи - международной коммуникационной платформы.

### Основные возможности базовой версии:

-   Просмотр программы и событий
-   Новостная лента
-   Информация о партнерах
-   Награды и премии
-   Контактная информация
-   Информационные страницы

## 🚀 Установка и запуск

### Требования:

-   PHP 8.2+
-   Composer
-   SQLite/MySQL
-   Node.js & npm (для фронтенда)

### Шаги установки:

1. **Перейти в директорию проекта:**

```bash
cd /Users/dchichmarev/Downloads/wps-laravel
```

2. **Установить зависимости PHP:**

```bash
composer install
```

3. **Установить зависимости npm:**

```bash
npm install
```

4. **Создать key приложения:**

```bash
php artisan key:generate
```

5. **Выполнить миграции БД:**

```bash
php artisan migrate
```

6. **Запустить dev сервер:**

```bash
php artisan serve
```

7. **В другом терминале запустить build процесс:**

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:8000`

## 📁 Структура проекта

```
wps-laravel/
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       ├── PageController.php      # Контроллер для статических страниц
│   │       ├── EventController.php     # Управление событиями
│   │       ├── NewsController.php      # Управление новостями
│   │       └── PartnerController.php   # Управление партнерами
│   └── Models/
│       ├── Event.php                   # Модель События
│       ├── News.php                    # Модель Новости
│       ├── Partner.php                 # Модель Партнера
│       └── Award.php                   # Модель Награды
├── database/
│   └── migrations/
│       ├── *_create_events_table.php
│       ├── *_create_news_table.php
│       ├── *_create_partners_table.php
│       └── *_create_awards_table.php
├── resources/
│   └── views/
│       ├── layouts/
│       │   └── app.blade.php           # Основной layout
│       ├── pages/
│       │   ├── about.blade.php
│       │   ├── for-participants.blade.php
│       │   ├── for-partners.blade.php
│       │   ├── award.blade.php
│       │   ├── press-center.blade.php
│       │   ├── contacts.blade.php
│       │   ├── mobile-app.blade.php
│       │   └── videos.blade.php
│       ├── program/
│       │   └── index.blade.php
│       ├── news/
│       │   └── index.blade.php
│       └── home.blade.php
├── routes/
│   └── web.php                         # Маршруты приложения
└── public/                             # Публичные файлы (CSS, JS, изображения)
```

## 🗄️ База данных

### Таблицы:

**events** - События и программа

-   title (string)
-   description (text)
-   category (string)
-   start_date (datetime)
-   end_date (datetime)
-   location (string)
-   content (text)
-   discussion_count (integer)
-   status (string)

**news** - Новости

-   title (string)
-   excerpt (text)
-   content (longText)
-   image (string)
-   published_at (datetime)
-   status (string)
-   views_count (integer)

**partners** - Партнеры

-   name (string)
-   description (text)
-   logo (string)
-   website_url (string)
-   category (string)
-   status (string)
-   order (integer)

**awards** - Награды и премии

-   title (string)
-   description (text)
-   winner_name (string)
-   winner_bio (text)
-   award_year (string)
-   award_type (string)
-   image (string)
-   achievement (text)

## 🔗 Маршруты

### Публичные маршруты:

-   GET `/` - Главная страница
-   GET `/about` - О Ассамблее
-   GET `/program` - Полная программа событий
-   GET `/program/{event}` - Детали события
-   GET `/news` - Список новостей
-   GET `/news/{news}` - Статья новости
-   GET `/for-participants` - Информация для участников
-   GET `/for-partners` - Информация для партнеров
-   GET `/award` - Награды и премии
-   GET `/press-center` - Пресс-центр
-   GET `/contacts` - Контакты
-   GET `/mobile-app` - Мобильное приложение
-   GET `/videos` - Видеотрансляции

### API маршруты (для администрирования):

-   `POST /api/events` - Создать событие
-   `GET /api/events` - Список событий
-   `GET /api/events/{event}` - Детали события
-   `PUT /api/events/{event}` - Обновить событие
-   `DELETE /api/events/{event}` - Удалить событие

Аналогично для `/api/news` и `/api/partners`

## 🎨 Дизайн

Проект использует:

-   **Bootstrap 5** для основного UI
-   **CSS переменные** для темизации
    -   `--primary: #1a3a52` (основной цвет)
    -   `--accent: #c41e3a` (акцент)
    -   `--light-bg: #f8f9fa` (светлый фон)

## 🔐 Безопасность

-   Защита CSRF токенами
-   Валидация входных данных
-   SQL injection защита (через ORM)
-   XSS защита

## 📝 Следующие шаги для разработки

1. **Добавить админ-панель:**

    - Создать admin роли
    - Миграция для users с ролями
    - CRUD интерфейсы для управления контентом

2. **Улучшить фронтенд:**

    - Добавить Vue.js или React
    - Создать интерактивные компоненты
    - Улучшить дизайн согласно оригинальному сайту

3. **Добавить функциональность:**

    - Система регистрации участников
    - Система комментариев и рейтинга
    - Поиск по всему сайту
    - Система уведомлений
    - Интеграция с соцсетями

4. **Оптимизация:**

    - Добавить кэширование
    - Оптимизировать изображения
    - SEO оптимизация
    - Мониторинг производительности

5. **Тестирование:**
    - Написать unit tests
    - Написать feature tests
    - Настроить CI/CD

## 🤝 Структура контроллеров

### PageController

Обрабатывает все информационные/статические страницы сайта.

Методы:

-   `home()` - Главная страница с превью
-   `about()` - О Ассамблее
-   `forParticipants()` - Для участников
-   `forPartners()` - Для партнеров
-   `award()` - Награды
-   `pressCenter()` - Пресс-центр
-   `contacts()` - Контакты
-   `mobileApp()` - Мобильное приложение
-   `videos()` - Видеотрансляции

### EventController, NewsController, PartnerController

RESTful контроллеры с полным CRUD функционалом.

## 🛠️ Утилиты и команды Artisan

```bash
# Создать новую миграцию
php artisan make:migration create_table_name

# Создать новый контроллер
php artisan make:controller ControllerName

# Создать новую модель
php artisan make:model ModelName -m

# Запустить миграции
php artisan migrate

# Откатить миграции
php artisan migrate:rollback

# Очистить кэш
php artisan cache:clear

# Очистить все кэши
php artisan optimize:clear
```

## 📞 Контактная информация проекта

**Адрес организации:**
127006, г. Москва, Успенский переулок, 4А

**Контактный телефон:**
+7 (495) 197-67-79

**Email:**
summit@eurasia-assembly.org

## 📄 Лицензия

Этот проект создан для Всемирной Общественной Ассамблеи.

---

**Версия:** 1.0.0 (Стартовая версия)  
**Последнее обновление:** Декабрь 2025
